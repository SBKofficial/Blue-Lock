user:
  name: "SBKofficial"
  github: "SBKofficial"

runtime:
  image: "python:3.11-slim"

commands:
  build:
    - "pip install --no-cache-dir -r requirements.txt"
  start: "python bot.py"

repository:
  branch: "main"
  auto_deploy: true
